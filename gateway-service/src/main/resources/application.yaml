server:
  port: 8761

spring:
  application:
    name: gateway-service

eureka:
  client:
    fetch-registry: false #It IS the registry
    register-with-eureka: false #Don't register itself - it is a sever
